// Built from  src/ff16r_strategy.cpp on Wed Aug 12 15:46:38 2020 using the scaffolder, from the strategy:  FF16r
// Built from  src/ff16_strategy.cpp on Fri Jul  3 08:14:35 2020 using the scaffolder, from the strategy:  FF16
//
// K93_Strategy is now a class template (K93_Strategy_<S>) with all method bodies
// defined inline in inst/include/plant/models/k93_strategy.h, so the active (AD)
// instantiation can see them. Nothing remains to compile out-of-line here; this
// TU is kept so the build's file list is unchanged.
#include <plant/models/k93_strategy.h>
